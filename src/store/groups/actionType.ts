const TypeName = "Groups";

const GROUP_ALL = `@${TypeName}_ALL`;
const GROUP_GET = `@${TypeName}_GET`;
const GROUP_ADD = `@${TypeName}_Add`;
const GROUP_EDIT = `@${TypeName}_Edit`;
const GROUP_REMOVE = `@${TypeName}_REMOVE`;
const GROUP_GET_BY_USER = `@${TypeName}_GET_BY_USER`;
const GET_TASKS_BY_USER = `@${TypeName}_GET_TASKS_BY_USER`;

export {
  GROUP_ALL,
  GROUP_GET,
  GROUP_ADD,
  GROUP_EDIT,
  GROUP_REMOVE,
  GROUP_GET_BY_USER,
  GET_TASKS_BY_USER,
};
